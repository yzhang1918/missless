{
  "name": "@missless/cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "missless": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm -r build && tsx --test ../../tests/integration/cli/*.test.ts"
  },
  "dependencies": {
    "@missless/contracts": "workspace:*",
    "@missless/core": "workspace:*",
    "@missless/rendering": "workspace:*"
  }
}
